Pour lancer le SERVEUR  (back-end) :
- Ouvrir le terminal (Cmd) dans le dossier Server 
- Lancer la commande ' npm install ' pour installer les packages utilisés lors du dévéloppement
- Lancer la commande 'npm start' pour lancer le serveur
Pour lancer l'Interface (Front-ent) :
- Ouvrir le terminal (Cmd) dans le dossier Client
- Lancer la commande ' npm install ' pour installer les packages utilisés lors du dévéloppement
- Lancer la commande 'npm start' pour lancer l'interface
